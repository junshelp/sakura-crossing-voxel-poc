# W007 Implementation Result

## Status

IMPLEMENTED; root verification PASS and retained production evidence complete.

## Summary

Added a same-session six-leg alternating benchmark with fixed 1600×900/DPR1
rendering, warm-up/sample boundaries, real uncapped timestamp deltas, route
markers, deterministic simulation reset, cancellation/visibility invalidation,
state restoration, downloadable versioned JSON, profile/source metadata,
threshold conclusions, diagnostic durations, and a gated Pages workflow.

## Files Changed

- `src/benchmark/index.ts` — schema, summaries, percentile, validation,
  threshold evaluation, download helper.
- `src/main.ts` — real renderer/session lifecycle, controls, metrics and restore.
- `src/simulation/index.ts` — public snapshot restore operation.
- `vite.config.ts` — truthful build commit/dirty metadata.
- `.github/workflows/pages.yml` — explicit `ENABLE_PAGES_DEPLOYMENT` gate.
- `README.md` — install/run/test/control/benchmark/limitations instructions.

## Acceptance Evidence

- Samples only include intervals wholly after warm-up and before sample end;
  mount and warm-up are excluded. Visibility loss and cancel invalidate results.
- Legs alternate Baseline/Voxel three times, reset to the deterministic train
  initial state, traverse Overview/Crossing/Railway, and restore mode, pose,
  marker, simulation, viewport, and DPR in `finally`.
- JSON records per-leg distributions and renderer metrics plus explicit unknown
  profile fields; GPU timer reports unsupported/not-measured with null values.
- Diagnostic mode is labeled `diagnostic=true` and cannot produce a formal
  conclusion. Threshold misses conclude `revise`; incomplete evidence is
  inconclusive. Visual Acceptance remains pending.
- Pages deploy job is gated by `vars.ENABLE_PAGES_DEPLOYMENT == 'true'`.

## Checks Run

- `npm test` — root PASS (13 files, 60 tests).
- `npm run typecheck` — PASS.
- `npm run build` — PASS (expected Three.js chunk warning only).
- `git diff --check` — PASS.
- Production E2E — root PASS (4/4, 42.6s).
- Two standard production captures — root PASS: Metal and SwiftShader, six
  alternating legs each, strict schema and browser error checks.

## Assumptions or Deviations

- Hardware and power context remain `unknown` unless entered by the capture
  operator; no GPU or performance values are invented.
- GPU asynchronous query collection is not implemented, so timer values remain
  null and are never presented as frame timing.

## Final Evidence and Limitations

- Root rejected the initial handoff and the incomplete domain correction.
  Domain/runtime workers made disjoint corrections; root then corrected the
  domain validation/sampling implementation and added adversarial cases before
  independent verification. Historical subhandoffs remain in W007-domain/runtime.
- `docs/benchmark/final-evidence-report.md` is the consolidated measured result.
  Primary draw calls decreased39.22%; p95 did not improve (0.09% regression).
  The decision is REVISE, not permission to expand. Secondary SwiftShader p95
  improved1.24%, but the profile is not an independent physical device.
- Captures record parent6c6a5fd and dirtyBuild=true; the exact measured JS hash
  is in the report. A favicon404 capture is preserved as excluded provenance.
- Human Visual Acceptance and public Pages deployment remain pending.
- GPU queries remain unmeasured/unsupported; headless frame pacing and
  uncontrolled background host activity limit performance generalization.
- Explanatory source/asset/bundle/build metrics do not prove upstream code savings.

## Reviewer Focus

Inspect benchmark lifecycle ownership in `runBenchmark`, exact interval
boundary semantics, six-leg ordering, and downloaded JSON metadata before the
standard evidence run.
