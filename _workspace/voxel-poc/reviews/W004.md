# W004 Review — PASS

## Verdict

PASS. Two correction cycles resolved the scene-layout, playability, resource,
and inherited railway-axis defects exposed by root review.

## Corrected Findings

1. Road and footway now run north/south across the local east/west rails; the
   footway at x=4.5 remains traversable outside the coarse crossing collider.
2. Barrier arms point inward from stable animation pivots.
3. The planet is opaque and lit, so the populated surface has readable curvature
   and far-side geometry is depth-occluded. The remaining dark sky arcs are the
   correctly placed utility wires between their poles.
4. The two-car train and collider start at x=-12,z=0 on the rail and appear in
   the fixed comparison composition.
5. Moving the north fence to z=29 untraps the railway marker. Tests prove first
   movement from every marker and a scripted crossing of the offset footway.
6. `equatorialRailwayPosition` now follows flat +x through the top-origin
   crossing and is tested for origin, east tangent, projection equivalence,
   radius, gauge, and closure.

## Root Verification

- `npm test` — PASS, 6 files / 31 tests
- `npm run typecheck` — PASS
- `npm run build` — PASS; only the expected Three.js chunk-size warning remains
- `npm run test:e2e` — PASS, 1 test
- `git diff --check` — PASS
- Baseline ownership search — PASS, no collider/interaction/simulation matches
- Headless Chromium fixed-view captures — PASS at 1440 x 900, no console errors

The captures show a solid curved surface, north/south road and footway, local
two-rail crossing, inward barriers, two-car train, buildings, blossom trees,
utility wires, and live telemetry. Human Visual Acceptance remains pending.
